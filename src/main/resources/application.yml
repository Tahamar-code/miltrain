spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/miltrain
    username: miltrain
    password: secret

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
